name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

      - name: Flavor check (audit committed generated data)
        run: npm run flavor:check

      - name: SRD content check (audit committed generated data)
        run: npm run srd:check

      - name: Check generated Worker bindings
        run: npm run cf:typegen:check

      - name: Build OpenNext Worker
        run: npm run cf:build

      - name: Test inside workerd
        run: npm run cf:test

      - name: Validate staging bundle
        run: npm run cf:dry-run

      - name: Check Worker startup budget
        run: npm run cf:startup
